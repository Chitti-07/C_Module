#include<stdio.h>
int main() {

//Reading single integer
/*
    int x;
    printf("Enter a number: \n");
    scanf("%d", &x);
    printf("You have entered: %d\n", x);

*/
//Reading Multiple numbers
/*
    int x, y;
    printf("Enter two number: ");
    scanf("%d %d", &x, &y);
    printf("Your X= %d\n Your Y=%d\n", x, y);
*/
//Reading Character
/*
    char ch;
    printf("Enter a character:");
    scanf("%c", &ch);
    printf("Your character is %c\n", ch);
*/
//No.of bytes consume by different data types

    // int x;
    // char ch;
    // float y;
    // double z;

    // printf("No of bytes consumed by %lu\n", sizeof(x));
    // printf("No of bytes consumed by %lu\n", sizeof(ch));
    // printf("No of bytes consumed by %lu\n", sizeof(y));
    // printf("No of bytes consumed by %lu\n", sizeof(z));

//Performing Arthimatic Operations
    // int x, y;
    // printf("Enter two numbers X Y: ");
    // scanf("%d %d", &x, &y);
    // printf("Addition of X and Y = %d\n", x+y);
    // printf("Subtraction of X and Y = %d\n", x-y);
    // printf("Multiplication of X and Y = %d\n", x*y);
    // printf("Division of X and Y = %f\n",(float)x/y);
    // printf("Division of X and Y = %g\n", (float)x/y);


    // int ch = 150;
    // unsigned int bc = 150;

    // printf("%hhd %hhd\n", ch, bc);
    // printf("%d %d\n", ch, bc);
    // printf("%hhu %hhu\n", ch, bc);
    // printf("%u %u\n", ch, bc);

    // int x;
    // char ch;
    // float f = 10;

    // scanf("%d*%c*%f", &x, &ch, &f);
    // //scanf("%c", &ch);

    // printf("%d %c %f", x, ch, f);

    
    int *ptr = NULL;
    printf("%lu", ptr);
}